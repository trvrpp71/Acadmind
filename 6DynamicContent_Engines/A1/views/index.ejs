<%- include('includes/head.ejs') %>

<%- include('includes/nav.ejs') %>

    <h1>User List</h1>

    <% if (users.length >0) { %>
        <ul>
            <% users.forEach((user) => { %>
                <li> <%= user.name %></li>
            <% }) %>
        </ul>
    <% } else { %>
        <h1>no users found</h1>
    <% } %>

    <%- include('includes/end.ejs') %>